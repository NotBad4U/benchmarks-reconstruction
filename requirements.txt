# Install into a local venv, never globally:
#
#     python3 -m venv .venv
#     ./.venv/bin/pip install -r requirements.txt
#
# That is the only Python dependency.  collects.py uses the standard library
# alone; numpy, pandas, tabulate and python-dateutil were needed only by the
# joblog parsers that have since been removed.
doit>=0.36

# Non-Python tools dodo.py shells out to: cvc5, carcara, lambdapi, hyperfine.
# On the ITU cluster, slurm/setup.job builds all of them into $HOME.
#
# Not listed on purpose: BenchExec.  runexec is Linux-only (it imports
# libc.so.6 and needs cgroup v2), so it cannot run on macOS.  The runexec
# variant of this pipeline is kept on the `v2` branch.
